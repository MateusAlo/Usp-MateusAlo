#include <stdio.h>
#include <math.h>
#include <time.h>
#define TAM 2



int igual(int matriz1[TAM][TAM], int matriz2[TAM][TAM])
{
    int i, j;
    for (i = 0; i < TAM; i++)
    {
        for(j = 0; j < TAM; j++)
        {
            if (matriz1[i][j] == matriz2[i][j])
            {
                
            }
        }
    }
}
